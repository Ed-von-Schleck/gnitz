[package]
name = "gnitz-core"
version = "0.1.0"
edition = "2021"

[features]
integration = []

[dependencies]
gnitz-wire     = { path = "../gnitz-wire" }
gnitz-protocol = { path = "../gnitz-protocol" }

[dev-dependencies]
libc = "0.2"
