[package]
name = "gnitz-core"
version = "0.1.0"
edition = "2021"

[features]
integration = []

[dependencies]
gnitz-expr  = { path = "../gnitz-expr" }
gnitz-wire  = { path = "../gnitz-wire" }
libc.workspace = true
lru         = "0.12"
rustls.workspace = true
webpki-roots = "1"

[dev-dependencies]
gnitz-test-harness = { path = "../gnitz-test-harness" }
# The loopback TLS peer the framing tests drive a real `connect_tls` against.
rcgen.workspace = true
tempfile.workspace = true

[lints]
workspace = true
